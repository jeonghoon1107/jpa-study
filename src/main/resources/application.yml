spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:~/test
    name: sa
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop